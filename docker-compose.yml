name: semantic-cache-proxy

services:

  backend:
    build: .
    ports: 
      - "8000:8000"

    env_file:
      - .env.docker

  redis:
    image: redis/redis-stack-server:latest
    ports:
      - "6379:6379"